const OWNER_PROFILE_URL = "https://github.com/Diego17cp";
const REPO_URL = `${OWNER_PROFILE_URL}/dialcalink`;

export const GITHUB_LINKS = {
	OWNER: OWNER_PROFILE_URL,
	REPO: REPO_URL,
	ISSUES: `${REPO_URL}/issues`,
	PULL_REQUESTS: `${REPO_URL}/pulls`,
	ACTIONS: `${REPO_URL}/actions`,
	RELEASES: `${REPO_URL}/releases`,
};
